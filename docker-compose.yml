# NOTA: Este archivo es para referencia local solamente
# Usar siempre docker-compose.yml desde la RAÍZ del proyecto
# Ubicación: D:\SWII\micro_servicios\docker-compose.yml

# Para desarrollo local sin Docker:
# npm install
# npm run dev

# Para desarrollo con Docker: usar docker-compose desde raíz:
# docker-compose up -d
